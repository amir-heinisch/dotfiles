{
	"name": "tmux",
	"description": "Best terminal multiplexer available.",
	"packages": {
		"default": "tmux",
		"dependencies": ["shell"]
	},
	"setdown": {
		"files": [ "${XDG_HISTORY_HOME}/tmux" ]
	}
}
